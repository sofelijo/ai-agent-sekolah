"""Flow handlers for ASKA.

Each module exposes a `handle_*` function that receives the
current Telegram update/context, normalized/raw user text,
and common metadata, and returns True if it handled the message.
"""

